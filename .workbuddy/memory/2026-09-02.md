
## UI 完整重构完成（续 8-29 会话）
- App.tsx 2517 行 → 226 行纯编排层；拆出 9 个组件 + 7 个域 hooks + types/constants/utils。
- 样式迁移到 tokens.css / base.css / layout.css / components.css（4px 栅格、token 化颜色/圆角/字阶），旧 App.css 已删除。
- P0 修复落地：分段表两行式布局（最低宽 858px→516px，1080 窗口不再横向溢出）、序号垂直居中、行 hover、voice-card/job-card 改 voice-card-main/job-card-main 两列结构、章节选中态单一定义。
- jobs 轮询做内容比对，无变化不重渲染。
- 新增 hooks：useNotifier/useAppSettings/useProjectSnapshot/useSegmentEditor/useCharacters/useVoiceProfiles/useStudioActions。
- 验证：pnpm build ✓、cargo check ✓、invoke 命令覆盖与重构前一致（export* 走 exportItem 字符串传参）。
- 修复了 markers/CSS 不匹配：audio-play/audio-mute 类名对齐、补 voice-panel-actions 与 settings-button 样式、章节标题改 strong。

## 首次 git 提交
- root-commit 0ce30f1，main 分支，80 文件 / 20966 行。
- 提交者身份是 git 自动生成的（XiiC <xiic@XiiCdeMacBook-Air.local>），用户尚未配置全局 user.name/email。
- 无远端；.github 的 CI workflow 已入库，待用户建远端后推送即可生效。
