# 2026-08-29

## 项目摸底（首次会话）

通读 xiic-voice-studio 全项目，建立认知基线：

- 定位：macOS 本地优先的中文有声书 / 多角色音频剧制作工作台（Tauri 2 + React 19 + Rust）。
- 代码规模：Rust 7083 行（src-tauri/src，含 tests.rs 1439 行），前端 App.tsx 2517 行 + App.css 1798 行。
- Rust 模块职责：domain（模型）/ storage（SQLite）/ importer（TXT·DOCX）/ ai（LLM 标注）/ tts（生成队列）/ audio（FFmpeg 归一与混音）/ settings / error。
- Tauri 命令 43 个，覆盖项目、章节、分段、角色、音色、任务队列、审听、导出、Keychain 设置。
- 持久化：本地项目文件夹为唯一真源（project.json + studio.sqlite + assets/ + exports/），API Key 存 macOS Keychain。
- 外部依赖：Mimo TTS（MiMo-V2.5 系列）、OpenAI-compatible LLM、系统 FFmpeg。
- 文档：docs/product-brief.md（产品与数据模型，含声音时间轴、AI 审听、供应商适配层设计）、docs/release.md（macOS 签名公证 secrets 清单）。

### 发现的待处理项

1. **git 仓库 0 commit**：仓库已初始化但全部文件未跟踪（`git status` 显示 ?? 全量），无任何提交记录，也没有远端。存在代码全丢失风险。
2. **src-tauri/target 占 8.8G**：已被 .gitignore 忽略，但磁盘占用可观，可考虑 `cargo clean`。
3. **USER.md 上下文过期**：`~/.workbuddy/USER.md` 里写的"当前工作区 TVApp"与实际项目不符，已更新为 xiic-voice-studio 并保留 TVApp 记录。
