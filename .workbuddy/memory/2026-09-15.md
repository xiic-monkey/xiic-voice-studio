# 2026-09-15

## git push：提交图标定稿 + 工作记忆
- 仓库 `xiic-monkey/xiic-voice-studio`（远程 ssh），分支 main。
- 执行 push 前发现**本地没有任何待推送提交**（与 origin 完全同步），工作树改动全是未提交内容，所以必须先 commit 再 push。
- **坑：仓库级与全局 git 身份都未配置**（`git config user.name/email` 均为空），直接 `git commit` 会失败。用 gh 登录账号构造 GitHub noreply 邮箱，仅在本仓库（local，不动全局）设置：
  - `xiic-monkey <24518439+xiic-monkey@users.noreply.github.com>`（ID 来自 `gh api user --jq .id`）
- 分成两个提交推送：
  - `d9c8682` 应用图标定稿：深蓝底 + 耳机 + 波形条 —— 53 文件，含 android/ios 平台图标与源图 `app-icon-source.png`
  - `2a9efcd` 更新工作记忆：预览与发布流程实测坑位 —— 6 文件
- 结果：`6a13191..2a9efcd main -> main`，工作树干净、与远程同步（0 0）。
